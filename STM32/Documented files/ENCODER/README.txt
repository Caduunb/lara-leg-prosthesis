READ ME file.

This program reads encoder position data as defined by SAMPLE_TIME and writes to SD card.
All encoder functions are inside encoder library in Inc (encoder.h) and Src (encoder.c) files.

Program reads and writes data inside the while infinite loop.